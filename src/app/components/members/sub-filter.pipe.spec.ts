import { SubFilterPipe } from './sub-filter.pipe';

describe('SubFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
